# Claude Code per-machine settings (machine-specific paths, not shared)
.claude/settings.local.json

# Editor / OS noise
.DS_Store

# Note: the linked repos are NOT tracked here — this workspace references them by
# their real path via `permissions.additionalDirectories` in `.claude/settings.json`
# (and lists them in <workspace-name>.code-workspace). Each repo has its own git.
